"use strict";
// function signature : specifying arguments and return value of function
let greetings;
greetings = (city, country, age) => {
    return 'Tokyo' + 'Japan' + (28).toString();
};
let calc;
calc = (one, two, name) => {
    if (name === "jake") {
        return one + two; // should return number
    }
    else {
        return one - two; // should return number
    }
};
let logDetails;
logDetails = (person) => {
    return 5;
};
