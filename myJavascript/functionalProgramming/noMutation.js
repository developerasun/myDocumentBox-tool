// one of the principals in functional programming is to not change
// things since changes might cause error/bug unintended.

// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
    return fixedValue+1;
  // Only change code above this line
}